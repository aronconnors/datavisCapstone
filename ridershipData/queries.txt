ARRIVALS:

SELECT
    month,
    day_of_week,
    hour_of_day,
    destination_station_complex_name,
    sum(estimated_average_ridership)
GROUP BY
    destination_station_complex_name,
    month,
    day_of_week,
    hour_of_day


DEPARTURES:

SELECT
    month,
    day_of_week,
    hour_of_day,
    origin_station_complex_name,
    sum(estimated_average_ridership)
GROUP BY
    origin_station_complex_name,
    month,
    day_of_week,
    hour_of_day